apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-deployment
  labels:
    app: jenkins-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-server
  template:
    metadata:
      labels:
        app: jenkins-server
    spec:
      containers:
      - name: jenkins
        image: jenkins/jenkins:lts
        ports:
        - containerPort: 8080 # Web UI
        - containerPort: 50000 # Agent connection
        volumeMounts:
        - name: jenkins-home
          mountPath: /var/jenkins_home
        # --- DooD FIX: Mount the Docker socket ---
        - name: docker-socket
          mountPath: /var/run/docker.sock
      volumes:
      - name: jenkins-home
        persistentVolumeClaim:
          claimName: jenkins-pvc
      # --- DooD FIX: Define the Docker socket volume ---
      - name: docker-socket
        hostPath:
          path: /var/run/docker.sock
          type: Socket
---
apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
spec:
  type: NodePort # Exposes Jenkins externally
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30000 # Choose a high port for external access
    name: http
  - port: 50000
    targetPort: 50000
    name: agent
  selector:
    app: jenkins-server